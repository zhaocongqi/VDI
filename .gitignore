# 子项目目录（独立 Git 仓库，不纳入主仓库跟踪）
/kagent/
/kubekey/
/kube-ovn/
/kube-vip/
/kubevirt/
/longhorn/

# Claude Code 配置（本地使用，不提交到远程）
.claude/

# CodeGraph 工具缓存
.codegraph/

# 常见临时文件
*.tmp
*.temp
*~
.DS_Store
Thumbs.db
desktop.ini
image.png

# IDE 配置
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace

# 构建产物（通用）
*.o
*.a
*.so
*.dylib
*.exe
*.bin

# 镜像文件
*.iso
*.qcow2
*.img
*.raw
*.vmdk

# 压缩包
*.tar.gz
*.tar.bz2
*.tar.xz
*.zip
*.tgz

# 日志文件
*.log

# 环境配置
.env
.env.local
.env.*.local

# 敏感文件（证书、密钥）
*.pem
*.key
*.crt
*.p12
*.pfx

# 进程与 socket 文件
*.pid
*.sock

# 部署敏感配置（含 SSH 密码等，仅保留 .template 模板）
deploy/k8s/inventory.yaml
deploy/hosts

# Python 缓存
__pycache__/

# QEMU/测试临时目录
tmp/
bin/
dist/
package/vdi-os/files/usr/bin/elemental
package/vdi-os/files/usr/bin/wharfie
package/vdi-os/files/usr/bin/elemental-iso-build

# 离线资源（由 build-bundle 脚本下载，不提交到 git）
package/vdi-os/iso/bundle/
package/vdi-cluster-repo/charts/
cache/
