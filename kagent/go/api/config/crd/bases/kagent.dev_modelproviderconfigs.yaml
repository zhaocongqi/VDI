---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: modelproviderconfigs.kagent.dev
spec:
  group: kagent.dev
  names:
    categories:
    - kagent
    kind: ModelProviderConfig
    listKind: ModelProviderConfigList
    plural: modelproviderconfigs
    shortNames:
    - mprov
    singular: modelproviderconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .status.modelCount
      name: Models
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          ModelProviderConfig is the Schema for the modelproviderconfigs API.
          It represents a model provider configuration with automatic model discovery.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelProviderConfigSpec defines the desired state of ModelProviderConfig.
            properties:
              endpoint:
                description: |-
                  Endpoint is the API endpoint URL for the provider.
                  If not specified, the default endpoint for the provider type will be used.
                pattern: ^https?://.*
                type: string
              secretRef:
                description: |-
                  SecretRef references the Kubernetes Secret containing the API key.
                  Optional for providers that don't require authentication (e.g., local Ollama).
                properties:
                  name:
                    description: Name is the name of the secret in the same namespace
                      as the ModelProviderConfig.
                    type: string
                required:
                - name
                type: object
              type:
                description: Type is the model provider type (OpenAI, Anthropic, etc.)
                enum:
                - Anthropic
                - OpenAI
                - AzureOpenAI
                - Ollama
                - Gemini
                - GeminiVertexAI
                - AnthropicVertexAI
                - Bedrock
                - SAPAICore
                type: string
            required:
            - type
            type: object
            x-kubernetes-validations:
            - message: endpoint must be a valid URL starting with http:// or https://
              rule: '!has(self.endpoint) || size(self.endpoint) == 0 || self.endpoint.startsWith(''http://'')
                || self.endpoint.startsWith(''https://'')'
            - message: secretRef is required for providers that need authentication
                (not Ollama)
              rule: self.type == 'Ollama' || (has(self.secretRef) && has(self.secretRef.name)
                && size(self.secretRef.name) > 0)
          status:
            description: ModelProviderConfigStatus defines the observed state of ModelProviderConfig.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ModelProviderConfig's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              discoveredModels:
                description: DiscoveredModels is the cached list of model IDs available
                  from this model provider
                items:
                  type: string
                type: array
              lastDiscoveryTime:
                description: LastDiscoveryTime is the timestamp of the last successful
                  model discovery
                format: date-time
                type: string
              modelCount:
                description: ModelCount is the number of discovered models (for kubectl
                  display)
                type: integer
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed ModelProviderConfig spec
                format: int64
                type: integer
              secretHash:
                description: SecretHash is a hash of the referenced secret data, used
                  to detect secret changes
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
