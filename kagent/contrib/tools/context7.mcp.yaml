apiVersion: kagent.dev/v1alpha1
kind: ToolServer
metadata:
  name: context7-mcp-latest
spec:
  config:
    stdio:
      args:
        - '-y'
        - '@upstash/context7-mcp@latest'
      command: npx
  description: 'context7 MCP with documentation'